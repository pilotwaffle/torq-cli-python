# P2 independent G1D validation record

Status: **PASS for the bounded P2 candidate-building scope**, verified 2026-09-16 America/Chicago (2026-09-17 UTC). Live-vendor authentication and P3 application are not included.

## Baseline and local provider inspection

- Captured source/assets/test hashes before P2 implementation in `tmp/p2-baseline-hashes.json` (160 files), preserving the approved but uncommitted P0/P1 increment.
- `python -m pytest tests -p no:cacheprovider`: 1012 passed, 13 skipped, 2 failed in 301.88s. Collection began before P2 files landed; this is baseline evidence, not a test of completed P2. Failures were the previous increment's undeclared `os` import boundary in `application/workspace.py` and stale release-note wording inconsistent with the existing local `v0.2.0` tag.
- Corrected `docs/releases/torq-cli-v0.2.0.md` to retain the historical candidate revision and distinguish the release tag from separate publication/attestation evidence. `python -m pytest tests/test_release_consistency.py -p no:cacheprovider`: 11 passed in 0.86s. Builder is correcting the explicit workspace import-boundary declaration.
- Read-only local Claude inspection: native `C:/Users/asdasd/.local/bin/claude.exe --version` returned `2.1.257 (Claude Code)`. `--help` confirmed tools-off, safe-mode, disabled session persistence/slash commands, plan permission mode and no-chrome flags. Help saved at `tmp/p2-claude-help.txt`. PATH resolves a batch wrapper, so a native executable is required for the new owned-process adapter. No auth command or provider request was invoked.

## Independent integration observations

- `tmp/p2-independent-run.py` uses the production task service, real Windows OwnedProcess, a clearly labelled local fixture provider, the real packaged structural checker, and real receipt authority. The corrected candidate changes subtraction to addition while the source hash remains unchanged. The service returns sealed verified candidate_ready with structural exit 0 and exactly two child launches.
- Latest existing-file report: `tmp/p2-independent-02037718/report.json`. Identical Start replay and replay after editing the draft return the original task, with no additional launch. Changing candidate bytes or adding an unrecorded file makes the result unready; restoring exact bytes/inventory restores readiness. Reconstructing the service preserves the verified result with zero redispatch.
- Empty-project report: `tmp/p2-independent-e7e5d0a5/report.json`. Zero source files and one allowed new output produced a verified generated file in the candidate; the original project remained empty. The same replay/tamper/inventory/restart checks passed. No pre-existing run or certificate was supplied.
- Early real browser/HTTP flow on port 8880 completed goal/scope -> Review plan -> Start building -> verified candidate, with a real fixture-provider and checker process. Saved goal and scope survived reload. Unauthenticated metadata returned 401, wrong-origin Start returned 403, and unknown request fields returned 409, with no extra process launch. Desktop and 320px layouts had no horizontal overflow. Initial console output contained only a missing favicon 404.
- Browser fault review exposed stale-plan enablement and loss of newer unsaved text; Builder is correcting these before the final browser gate. Early screenshots `tmp/p2-browser-first-desktop.png` and `tmp/p2-browser-first-mobile.png` are interim, not release evidence.

## Subsequent integration checks

- Production CLI startup succeeded with `fleet --serve --task-project calculator=... --task-provider claude --task-model sonnet --task-claude-bin ...`, without a run-root, state-root, or work-root argument. A child-shell `LOCALAPPDATA` override kept default state/work creation inside an isolated temporary directory. The bootstrap opened New task directly, with the heading "What would you like to build?" and provider authentication explicitly `not_checked`. No Start or vendor request was issued on that server.
- The updated source browser completed another actual fixture build, displayed `Candidate verified` and structural exit 0, and let history reopen that verified result. Saved goal and input/output scope survived reload. Editing after review disabled Start immediately. Delaying a plan request while editing left Start disabled and preserved the newer saved text. The 320px viewport had no horizontal overflow.
- A first-load interaction exposed a further initialization race: new text survived the response guard but the authoritative draft revision was initially skipped, causing a CAS conflict. A lost HTTP response also exposed the need for a narrow authenticated idempotent Start replay path when the rotated cookie is lost. These are being checked before the final gate.
- First broad P2 suite: **1040 passed, 13 skipped, 1 failed** in 106.53s. The failed chat-origin HTTP request was aborted by Windows. An isolated chat HTTP check during the next edit also caught a misplaced required `replay_only` argument. Neither result is represented as a passing final suite; the final rerun follows the HTTP corrections.

## Final verification

- Final full suite: `python -m pytest tests -p no:cacheprovider --basetemp E:/Torq-CLI/tmp/p2-final-pytest-3` — **1042 passed, 13 skipped in 103.89s**. The preceding full pass was 1041 passed/13 skipped; the final UI correction added a second-task regression. The final suite includes it. Windows owner-only fixture permissions required execution outside the restricted sandbox; no fixture permission checks were bypassed.
- `python -m ruff check src tests`: passed. `python -m mypy src`: passed, 90 source files. `git diff --check`: passed. Builder also passed Node syntax/runtime/DOM checks and the wheel build.
- Installed the final wheel into a fresh local target with `pip install --no-deps --no-index --target tmp/p2-wheel-install-final ...`. Launched the browser fixture with Python `-I`; the printed imported module was `tmp/p2-wheel-install-final/torq_cli/__init__.py`, not the source checkout. All packaged source/assets match the checkout bytes.
- Final wheel: `tmp/p2-final-dist/torq_cli-0.2.0-py3-none-any.whl`, SHA-256 `ef547cd476261eec58c5dc7b4a95db5edc4176edd0378035f08b541ae5afe10f`.
- Packaged browser initialization: delayed draft GETs by 1000ms, for both new and existing drafts. Fields remained disabled until the authoritative draft loaded; saved goal and exact scope survived reload. Deleting a draft survived reload, and recreating it saved revision 4 without an ABA conflict.
- Packaged lost-response test: allowed the server to accept Start, deliberately restored the old cookie and aborted delivery, then used Start again. The two request bodies were identical. Replay recovered the accepted task, with **one provider plus one checker launch**, and the source file stayed unchanged. Reload resumed `GENERATING`, followed by verified `candidate_ready` and structural exit 0.
- That test exposed stale capabilities after a resumed task ended. Builder fixed terminal capability refresh and added a durable DOM regression. On the rebuilt final wheel, the browser resumed an active task, reached a verified candidate, then reviewed and started a **second task without reloading**. Stop confirmed `CANCELLED`. Total launches were three: first provider/checker plus the cancelled second provider. `tmp/p2-wheel-browser-final/validation.json` records wheel parity, hash, launch count, and source immutability.
- History reopened the verified result. Final screenshots are `tmp/p2-wheel-desktop.png` and `tmp/p2-wheel-mobile.png`. Desktop 1440px and mobile 320px had no horizontal overflow. Dark theme, reduced-motion preference, focus outline, and 200% CSS zoom were checked; CSS zoom is not claimed as a native browser zoom audit. The deliberately aborted request produced an expected network error; it was not an application exception.
- The source task-only CLI launch required no run-root, certificate, or explicit state/work roots. Live authentication remained `not_checked` throughout that startup-only check.
- All root-owned fixture servers were stopped and the browser returned to `about:blank`. No commit, push, merge, deployment, or primary-project application was performed. Existing unrelated worktree artifacts were preserved.

Independent design approval is recorded in `2026-09-16-dashboard-p2-g1r-review.md`; final audit approval is recorded separately in `2026-09-16-dashboard-p2-g2a-review.md`. Builder handoff: `2026-09-16-dashboard-p2-builder-handoff.md`.

Fixture-generated files and local CLI help cannot establish live-vendor authentication or model behavior. Structural checks cannot establish unit-test correctness or primary-project application. Report those limitations separately from the implementation result.
